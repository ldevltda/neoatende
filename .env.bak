# Identidade da stack (só estética / labels)
STACK_NAME=dev
COLOR=#08a0db
TAB_NAME=Atendechat Dev

# URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8080

# Limites e reservas (Compose usa assim: ${VAR} e o yml acrescenta "M" onde precisa)
# CPUs aceitam float (ex.: 0.50). Memória use números inteiros (porque no yml vira 512M, 1024M etc).

# FRONTEND
FRONTEND_CPU_LIMIT=0.50
FRONTEND_CPU_RESERVE=0.25
FRONTEND_MEM_LIMIT=512
FRONTEND_MEM_RESERVE=256

# BACKEND
BACKEND_CPU_LIMIT=1.00
BACKEND_CPU_RESERVE=0.50
BACKEND_MEM_LIMIT=1024
BACKEND_MEM_RESERVE=512

# POSTGRES
POSTGRES_CPU_LIMIT=0.50
POSTGRES_CPU_RESERVE=0.25
POSTGRES_MEM_LIMIT=512
POSTGRES_MEM_RESERVE=256

# REDIS
REDIS_CPU_LIMIT=0.25
REDIS_CPU_RESERVE=0.10
REDIS_MEM_LIMIT=128
REDIS_MEM_RESERVE=64

# (Opcional) PIX/Gerencianet — deixe em branco no dev se não for usar
GERENCIANET_CLIENT_ID=
GERENCIANET_CLIENT_SECRET=
GERENCIANET_PIX_KEY=

# .env (na raiz do repo)
DATABASE_URL=postgres://postgres:postgres123@postgres:5432/codatende
# ou, se preferir usar variáveis discretas:
# DB_DIALECT=postgres
# DB_HOST=postgres
# DB_PORT=5432
# DB_USER=user
# DB_PASS=senha
# DB_NAME=db_name

# REDIS_HOST=redis
# REDIS_PORT=6379
# (Opcional) REDIS_URI=redis://redis:6379
TZ=America/Sao_Paulo
NODE_ENV=development
