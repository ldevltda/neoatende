export const handleAckUpdate = (updates: any[]) => {
  console.log("handleAckUpdate chamado", updates.length);
};

export const handleReceiptUpdate = (receipts: any[]) => {
  console.log("handleReceiptUpdate chamado", receipts.length);
};
